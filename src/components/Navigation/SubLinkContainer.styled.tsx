import { colors } from 'components/styles/colors'
import styled from 'styled-components'

export const SubLinkContainerStyled = styled.div`
  color: ${colors.white};
  padding: 0 0 0 10px;
  opacity: 0.6;
`
